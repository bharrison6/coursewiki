# Generated single-file bundles. Regenerable from source with -Bundle, and not
# needed to clone or deploy — the tracking policy says generated, non-clone-
# necessary output stays out of git and rides OneDrive instead.
bundle/

# Working notes. This repo is PUBLIC and holds only the website, so the scope's
# artifacts use the gitignored-in-tree class: OneDrive syncs and backs them up,
# git does not publish them.
.artifacts/

# Local render/verification scratch
docs/_*.html

# Windows / editor noise
Thumbs.db
desktop.ini
.DS_Store
