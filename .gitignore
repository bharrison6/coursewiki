

# Working notes. This repo is PUBLIC and holds only the website, so the scope's
# artifacts use the gitignored-in-tree class: OneDrive syncs and backs them up,
# git does not publish them.
.artifacts/

# Local render/verification scratch
docs/**/_*.html

# Windows / editor noise
Thumbs.db
desktop.ini
.DS_Store
